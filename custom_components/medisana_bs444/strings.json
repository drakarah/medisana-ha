{
  "config": {
    "step": {
      "bluetooth_confirm": {
        "description": "Do you want to set up {name}?",
        "data": {
          "use_time_offset": "Use time offset (required by BS410 and BS444)"
        }
      },
      "user": {
        "description": "Choose a discovered Medisana scale to set up.",
        "data": {
          "address": "Device",
          "use_time_offset": "Use time offset (required by BS410 and BS444)"
        }
      }
    },
    "abort": {
      "no_devices_found": "No unconfigured Medisana scales found.",
      "already_configured": "Device is already configured"
    }
  },
  "entity": {
    "sensor": {
      "weight": { "name": "Weight user {user_id}" },
      "bmi": { "name": "BMI user {user_id}" },
      "kcal": { "name": "kcal user {user_id}" },
      "fat": { "name": "Fat user {user_id}" },
      "tbw": { "name": "Water user {user_id}" },
      "muscle": { "name": "Muscle user {user_id}" },
      "bone": { "name": "Bone user {user_id}" },
      "age": { "name": "Age user {user_id}" },
      "size": { "name": "Size user {user_id}" }
    },
    "binary_sensor": {
      "male": { "name": "Male user {user_id}" },
      "female": { "name": "Female user {user_id}" },
      "high_activity": { "name": "High activity user {user_id}" }
    }
  }
}
